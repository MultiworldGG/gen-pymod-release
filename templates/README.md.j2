# worlds.{{ apworld }} — wheel artifact

This is an **artifact** auto-built by [MultiworldGG/gen-pymod-release](https://github.com/MultiworldGG/gen-pymod-release).
It contains a pip-installable layout of the `{{ apworld }}` world from this repo.

- **World:** {{ game_name }}
- **Version:** {{ world_version }}
- **Source ref:** `{{ source_ref }}`
- **Built:** {{ built_at }}

## Install

```
pip install https://github.com/{{ caller_repo }}/releases/download/{{ release_tag }}/{{ dist }}-{{ world_version }}-py3-none-any.whl
```

After install, `worlds.{{ apworld }}` is importable in any environment that has the
MultiworldGG worlds-namespace stub installed (the MultiworldGG client provides it).

## How this branch is structured

```
pyproject.toml
src/
  worlds/
    {{ apworld }}/
      ...the world's Python source...
```

This is a pip-installable [namespace-package](https://peps.python.org/pep-0420/)
contribution under the shared `worlds.` namespace. It coexists with all other
per-world install branches because none of them ship `worlds/__init__.py`.

## Important

- This artifact is uploaded on every release of this world.
- The `{{ release_tag }}` **tag** is immutable — pin
  to it for reproducibility. Pinning to the release tip
  (`{{ release_tag }}`) gets the latest published version.
- Source for this build lives at `{{ source_ref }}` on the caller repo.
